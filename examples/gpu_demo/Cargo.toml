[package]
name = "example-gpu-demo"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
shrew = { workspace = true, features = ["cuda"] }
shrew-core.workspace = true
shrew-cpu.workspace = true
shrew-cuda.workspace = true
